# Agent Implementation Progress

Tracks completion of milestones from `atom_plan_agent_implementation_guide.md`.

## Milestones

| Milestone                                               | Status        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **K1** — Runtime Key Family Schema                      | **Completed** | Added `KeyFamilyHierarchy`, `input_type?: SemanticType<Payload>`, and `hierarchy` to `KeyFamily`. Updated `defineKeyFamily`, `entityKeyFamily`, `collectionKeyFamily`. Schema-driven and generic-only forms both work. 669 tests passing.                                                                                                                                                                                                                                             |
| **K2** — Key Pattern Validation and Diagnostics         | **Completed** | Added `keyPayloadFields` helper extracting field names from struct `SemanticType`. Expanded `checkReactivity` to validate mutation invalidation patterns, query key payloads, and action invalidation patterns against known fields. Updated lifecycle to pass query/action functions. 672 tests passing.                                                                                                                                                                             |
| **K3** — Semantic Key Hierarchy, Registry, and Batching | **Completed** | Added `KeyInvalidationSemantics`, `ReactiveRegistry`, and `semantics` to `KeyFamily` with hierarchy-derived defaults. Added `defineReactiveRegistry`, `InferReactiveRegistryFamilies`, `InferRegistryFamily`. Wired into `GenContext`, `gen.reactivity.registry`, binders, and namespace types. 674 tests passing.                                                                                                                                                                    |
| **E1** — Constant vs Expression Key Plans               | **Completed** | Replaced `KeyExpression` and `KeyPatternExpression` with discriminated unions (`ConstantKeyExpression`, `StaticKeyExpression`, `ConstantKeyPatternExpression`, `StaticKeyPatternExpression`). Updated `keyExpr`, `keyPatternExpr`, normalizers in `function.ts`, graph derivation, `checkReactivity`, and tests. Narrowed union access with `kind` checks. 674 tests passing.                                                                                                         |
| **A1** — Action Input/Result-Aware Invalidation Types   | **Completed** | Added `MutationKeyContext<In, Out>`, updated `ActionReactivity` to parameterize over input and output, changed `ActionFunction.reactivity` to `ActionReactivity<In, Out>`. Updated `lowerLegacyInvalidations` and `defineActionFunction` normalizers to use the new context type. Added type tests proving invalidation metadata carries `MutationKeyContext`. 674 tests passing.                                                                                                     |
| **G1** — Typed Reactive Graph Edges                     | **Completed** | Replaced flat `ReactiveGraphEdge` with discriminated unions (`ReadsKeyEdge`, `WritesEntityEdge`, `InvalidatesKeyEdge`, `WrapsQueryEdge`, `WrapsActionEdge`, `ComposesResourceEdge`, `ReadsResourceEdge`, `RouteLoadsEdge`, `RouteSubmitsEdge`, `FormSubmitsEdge`, `EmitsEventEdge`, `SubscribesEventEdge`). Updated `deriveReactiveGraph` and all graph query helpers. Updated `effect-atom.ts` and `tanstack-query.ts` targets. Updated all test edge assertions. 674 tests passing. |
| **DERI1** — Conservative Derived Dependency Pass        | **Completed** | Added `ReadDependency`, `WriteDependency`, `DerivedInvalidation` types. Implemented `deriveQueryReadDependencies`, `deriveActionWriteDependencies`, `deriveConservativeInvalidations` helpers. Updated `deriveReactiveGraph` to include derived edges with `derived`/`conservative` confidence/precision. 674 tests passing.                                                                                                                                                          |
| **SCOPE1** — Tracking Scope Metadata                    | **Completed** | Added `TrackingScope`, `TrackingScopeKind`, `TrackingScopeNode`. Added `defineTrackingScope`. Updated `GenContext` to track scopes. Updated `deriveReactiveGraph` to include `scope_reads_key` and `scope_reads_query` edges. Updated `ReactiveGraphNode` interface and `ReactiveGraphEdge` union. 674 tests passing.                                                                                                                                                                 |
| **R1** — Resource State and Resource Diagnostics        | **Completed** | Replaced `ResourceState` with a discriminated union (`initial`, `loading`, `success`, `refreshing`, `failure`, `defect`). Added `reactivity:resource-query-unkeyed` diagnostic for resources using unkeyed queries when a refresh plan explicitly requests invalidation. Added tests for invalid states and the new diagnostic. 675 tests passing.                                                                                                                                    |
| **IA1** — Static Invalidation Action Node               | **Completed** | Added `InvalidateOperation` as a static action node. Updated `ActionExpr.operations` to union of `WriteOperation                                                                                                                                                                                                                                                                                                                                                                      | InvalidateOperation`. Added `buildActionInvalidate`and exported via`gen.func.buildActionInvalidate`. Updated graph derivation to create `invalidates_key` edges from static invalidation nodes in action bodies. Added tests proving sequence composition works with invalidation nodes. 675 tests passing. |
| **C1** — CRUD Key Alignment                             | **Completed** | Updated `entityKeyFamily` and `collectionKeyFamily` in `src/reactivity/reactivity.ts` to include `input_type` where possible. Updated `deriveCrud` in `src/gen/binders.ts` to use explicit key families and avoid duplicating default key families. Added tests for custom key families and duplicate key families avoidance. 677 tests passing.                                                                                                                                      |
| **RR1** — Rule-Derived Reactivity Safety                | **Completed** | Updated `deriveRuleInvalidationPlans` to record `appliedPrecision: "broad"`. Renamed `rules-reactivity:affected-set-unknown` to `rules-reactivity:patchable-advisory-only` inside the patchable check. Renamed `rules-reactivity:dependency-not-extractable` to `rules-reactivity:complex-dependency-reduces-precision`. Updated tests in `tests/rules-reactivity.test.ts` and `tests/rules-reactivity-depth.test.ts`. 677 tests passing.                                             |
| **T0** — Target Adapter Stabilization                   | **Completed** | Updated `generateEffectAtomArtifacts` and `generateTanstackQueryArtifacts` to check for required `stable_id` (symbol metadata) and emit warnings when absent. Verified target generators correctly extract new G1 typed edges. Updated tests to expect the missing metadata warnings for standalone node generation. 677 tests passing.                                                                                                                                               |
| **PE1** — Capability/Fallback Metadata Seed             | **Completed** | Added `CapabilityTier` and `EnhancementPlan` to `src/core/target.ts`. Added `enhancement?: EnhancementPlan` to `ReactiveResource` in `src/reactivity/reactivity.ts`. Allows resources to define explicit baseline, preferred, and fallback capability tiers. 677 tests passing.                                                                                                                                                                                                       |
| **D1** — Documentation and Plan Synchronization         | **Completed** | Updated `atom_plan.md` to reflect that reactivity, rules, router, hydration, services, and admin are implemented. Preserved historical details but added a "Current Hardening Priority" section pointing to this guide. Summarized `spec/atom.txt` principles.                                                                                                                                                                                                                        |

## Verification Commands

```sh
vp check
vp test
```

## K1 Details

- **Files changed:**
  - `src/reactivity/reactivity.ts` — added `KeyFamilyHierarchy`, extended `KeyFamily`, updated constructors.
  - `tests/reactivity.test.ts` — added runtime tests for schema-driven families and hierarchy defaults.
  - `tests/reactivity.test-d.ts` — added type tests proving schema-driven inference works without explicit generics.
- **Acceptance criteria:** all met. `vp check` and `vp test` pass.
