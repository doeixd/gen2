/**
 * Entity module re-export barrel.
 *
 * Aggregates all public symbols from {@link ./entity.ts} so consumers can import
 * them via `import { ... } from "./entity"`.
 */

export * from "./entity.ts";
